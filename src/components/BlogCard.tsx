import React from "react";

interface BlogCardProps {
  title: string;
  authorName: string;
  publishedAt: string;
  content: string;
}

const BlogCard = ({title}) => {
  return <div></div>;
};

export default BlogCard;
